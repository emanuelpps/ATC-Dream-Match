import React from "react";

type Props = {};

const TeamsWrapper = (props: Props) => {
  return (
    <div id="teams-wrapper" className="w-[100%] flex items-center justify-center  h-screen bg-center bg-cover bg-home-background">
      <div className="w-full h-screen border-2 border-black">jdjsdjds</div>
    </div>
  );
};

export default TeamsWrapper;
